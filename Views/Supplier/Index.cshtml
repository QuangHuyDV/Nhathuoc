@model IEnumerable<Nhathuoc.Models.Supplier>

@{
    ViewData["Title"] = "Supplier List";
    Layout = "~/Views/Shared/MyLayout.cshtml";
}

<div class="container">
    <h2>Danh sách nhà cung cấp</h2>
    <a asp-action="Create" class="btn btn-primary mb-2">Tạo nhà cung cấp</a>

    <table class="table table-bordered table-striped">
        <thead>
            <tr>
                <th>Tên nhà cung cấp</th>
                <th>Email</th>
                <th>Số điện thoại</th>
                <th>Địa chỉ</th>
                <th>Thao tác</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.SupplierName</td>
                    <td>@item.Email</td>
                    <td>@item.Phone</td>
                    <td>@item.Address</td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.SupplierId" class="btn btn-sm btn-warning">Chỉnh sửa</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
    <ul class="pagination">
        @for (int i = 1; i <= ViewBag.pageNum; i++)
        {
            <li class="page-item @(ViewBag.currentPage == i ? "active" : "")">
                <a class="page-link" asp-action="Index" asp-route-mid="@ViewBag.mid" asp-route-page="@i">@i</a>
            </li>
        }
    </ul>
</div>
